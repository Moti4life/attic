//const { calculateTip, celsiusToFahrenheit, fahrenheitToCelsius, add } = require('../src/math')

/* test('should return total with tip', () => {
    const total = calculateTip(10, .3)
    expect(total).toBe(13)

    //user  expect(value).toBe(expectedvalue)

    // if(total !== 13){
    //     throw new Error('tip should be 13 but value is: ' + total)
    // }
    //console.log(total)
})

test('with default tip', () => {
    const total = calculateTip(10)
    expect (total).toBe(12.5)
    //console.log(total)
}) */

/* test("Should convert 32 F to 0 C", () => {
    const converted = fahrenheitToCelsius(32)
    expect(converted).toBe(0)
})  


test("Should convert 0 C to 32 F", () => {
    const converted = celsiusToFahrenheit(0)
    expect(converted).toBe(32)
})  */

/* test('async demo', (done) => {
    setTimeout( () => {
        expect(1).toBe(2)
        done()
    }, 2000)
    
}) */

//promise chaining
// test('this is add', (done) => {
//     add(2, 3).then((sum) => {
//         expect(sum).toBe(5)
//         done()
//     })
// })


// //async await
// test('should add nums async await', async () => {
//     const sum = await add(11, 22)
//     expect(sum).toBe(33)
// })




//====================
/* test('hello', () =>{
    
}) 

test('this will fail', () => {
    throw new Error('failed the teststst')
}) */

/* const mongoose = require('mongoose')
afterAll(() => {
    mongoose.connection.close();
}) */